@model Testing.Models.BlockedReasonList

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>BlockedReasonList</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.BlockedReasonValue)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BlockedReasonValue)
            @Html.ValidationMessageFor(model => model.BlockedReasonValue)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.BlockedReasonDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.BlockedReasonDescription)
            @Html.ValidationMessageFor(model => model.BlockedReasonDescription)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
