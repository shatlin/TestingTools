@model IEnumerable<Testing.Models.CycleDate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cycle.CycleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateInCycle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DayName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cycle.CycleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateInCycle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DayName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
