@model Testing.Models.CycleDate

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CycleDate</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.CycleId, "Cycle")
        </div>
        <div class="editor-field">
            @Html.DropDownList("CycleId", String.Empty)
            @Html.ValidationMessageFor(model => model.CycleId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DateInCycle)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DateInCycle)
            @Html.ValidationMessageFor(model => model.DateInCycle)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.DayName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DayName)
            @Html.ValidationMessageFor(model => model.DayName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
