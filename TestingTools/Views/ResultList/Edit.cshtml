@model Testing.Models.ResultList

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ResultList</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.ResultName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ResultName)
            @Html.ValidationMessageFor(model => model.ResultName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ResultDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ResultDescription)
            @Html.ValidationMessageFor(model => model.ResultDescription)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
