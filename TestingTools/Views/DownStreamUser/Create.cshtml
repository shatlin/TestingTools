@model Testing.Models.DownStreamUser

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DownStreamUser</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DownStreamId, "DownStream")
        </div>
        <div class="editor-field">
            @Html.DropDownList("DownStreamId", String.Empty)
            @Html.ValidationMessageFor(model => model.DownStreamId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.UserId, "User")
        </div>
        <div class="editor-field">
            @Html.DropDownList("UserId", String.Empty)
            @Html.ValidationMessageFor(model => model.UserId)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
