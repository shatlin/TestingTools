@model Testing.Models.ReadyForReleaseList

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ReadyForReleaseList</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.ReadyForReleaseValue)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ReadyForReleaseValue)
            @Html.ValidationMessageFor(model => model.ReadyForReleaseValue)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ReadyForReleaseDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ReadyForReleaseDescription)
            @Html.ValidationMessageFor(model => model.ReadyForReleaseDescription)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
